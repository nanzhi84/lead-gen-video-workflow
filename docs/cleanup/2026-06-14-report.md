# 仓库清理 · 代码类候选报告

- 日期：2026-06-14
- 生成：`repo-cleanup` 工作流（report 模式，42 agent / 700 工具调用 / 9.5min）
- 分析基线：**主 checkout 的 detached HEAD `83d018d`**（领先 `main` 分支 ref `d5876ec` 共 18 个 commit）
- 复核：每条候选经独立 skeptic 对抗式复核；保守默认 flag-only/keep

## 汇总

候选 **29** → 确认可删 **23**、待人工确认 **3**、已否决 **3**

> ⚠️ **基线提示**：本报告对着领先态 83d018d 生成。落地前每条都按下方「对 main 分支有效性」分档处理：仅领先态的条目只有在领先工作合入 main 后才适用；标「需复核」的需在目标基线重新定位行号。


---
# A. 确认可删（已过对抗复核）

## A1 · 死代码 dead（17）

### [core-contracts-3] `packages/core/contracts/artifacts.py`:88-93  —  AnnotationEditView
- 类别/类型：dead · unused class (Pydantic model)
- 对 main 分支有效性：**main 上需复核(领先态改过此文件)**
- 置信：medium  · 残余风险：low
- 证据：vulture flags unused class. grep across .py/.ts/.tsx/.json (excluding node_modules/.venv) for `AnnotationEditView`: only the definition here + 2 mentions in docs/audit/spec-gap-audit-2026-06-11.json (descriptive audit text, not code; audit itself notes the frontend annotation editor UI does not exist). Not in __init__ __all__, not in ArtifactSchemaRegistry.default() entries dict (lines 355-381), 0 code references.
- skeptic 反驳：none found — only "future intent" reservation: docs/spec (23.9/32.5) still describe AnnotationEditView as the intended annotation-editor contract, and the live feature instead uses a different class (AnnotationEditorVm in _media.py). The audit JSON itself (docs/audit/spec-gap-audit-2026-06-11.json:222,1614) explicitly records these spec types as absent/unimplemented. So AnnotationEditView is a vestigial spec-era stub, not a currently-wired contract. No code reference, registration, or test depends on it.
- **建议改动**：Delete class AnnotationEditView (and its only consumers AnnotationTimelineRow/QualityEventRow/FieldUiMetadata if they become orphaned — verify those independently).
- 复核执行的检查（10 项）：Read full artifacts.py (def at line 102; siblings AnnotationTimelineRow/QualityEventRow/FieldUiMetadata at 77/86/94 used only by AnnotationEditView)；grep AnnotationEditView across .py/.ts/.tsx/.js/.jsx/.json/.yaml/.md excluding node_modules/.venv/dist — only self-definition + docs (spec md + audit/milestone json), zero code refs；grep each sibling symbol (AnnotationTimelineRow/QualityEventRow/FieldUiMetadata/AnnotationPatchRequest) — siblings referenced only inside AnnotationEditView body；Checked __init__.py re-exports: AnnotationEditView and 3 siblings NOT re-exported (no `as` alias entries)；Checked ArtifactSchemaRegistry.default() entries (lines 387-413): not registered as any ArtifactKind；Checked digital_human.py import block (only file importing artifacts symbols): imports 20 symbols, none are AnnotationEditView or siblings…

### [core-contracts-4] `packages/core/contracts/artifacts.py`:96-100  —  AnnotationPatchRequest
- 类别/类型：dead · unused class (Pydantic model)
- 对 main 分支有效性：**main 上需复核(领先态改过此文件)**
- 置信：medium  · 残余风险：low
- 证据：vulture flags unused class. Full-repo grep (.py/.ts/.tsx/.json minus node_modules/.venv) yields only the definition here + one mention in docs/audit JSON (audit text). Not in __all__, not in ArtifactSchemaRegistry.default() registry, 0 code references.
- skeptic 反驳：none found
- **建议改动**：Delete class AnnotationPatchRequest.
- 复核执行的检查（5 项）：grep whole repo；read file；checked registry；checked tests；checked init exports

### [core-contracts-5] `packages/core/contracts/artifacts.py`:229-237  —  PortraitSegment
- 类别/类型：dead · unused class (Pydantic model)
- 对 main 分支有效性：**main 上需复核(领先态改过此文件)**
- 置信：medium  · 残余风险：low
- 证据：vulture flags unused class. Full-repo grep yields only the definition + one descriptive mention in docs/audit JSON. PortraitPlanArtifact.segments (line 245) is typed `list[dict[str, Any]]`, not list[PortraitSegment], so this model is not referenced even structurally. Not in __all__, not in registry, 0 code references.
- skeptic 反驳：none found. The strongest counter-argument is that docs/树影_Cutagent_CleanSlate重写Spec_v3_2026-06-11.md:2748-2761 describes the intended design where PortraitPlanArtifact.segments is typed list[PortraitSegment]. But the implementation deliberately diverged: PortraitPlanArtifact.segments (artifacts.py:248) is typed list[dict[str, Any]], and the only construction site (digital_human.py:1313-1329) emits raw dicts whose keys (start_sec/end_sec) don't even match PortraitSegment's fields (start/end), so the model is never instantiated even structurally. The spec mention is descriptive, not a live reference.
- **建议改动**：Delete class PortraitSegment.
- 复核执行的检查（11 项）：grep PortraitSegment across whole repo (all file types, excl .git): only the class definition + 2 descriptive docs mentions (audit JSON, spec md)；grep PortraitSegment in *.py: sole hit is artifacts.py:232 definition；checked __all__ in artifacts.py: none present；checked __init__.py re-exports: no PortraitSegment re-export, no 'from .artifacts import *' wildcard; only PortraitOptions (different symbol) re-exported；inspected artifact validation registry _models dict (artifacts.py:390-413): PortraitSegment not registered; validate_artifact only validates registered models via model_validate；verified PortraitPlanArtifact.segments typed list[dict[str, Any]] (line 248), not list[PortraitSegment] -> not referenced structurally…

### [core-storage-1] `packages/core/storage/sqlalchemy_idempotency.py`:54-58  —  cleanup_expired
- 类别/类型：dead · unused method
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：high  · 残余风险：low
- 证据：vulture flags it (60%); grep 'cleanup_expired' over ./packages ./apps ./tests yields 0 references outside its own definition. The idempotency repository is wired into apps/api only for get/put (apps/api/dependencies.py:130 calls .get/.put via common.idempotency_repository); no cron/scheduler/worker task ever invokes the TTL sweeper. No getattr/string dynamic dispatch found. Built-but-never-wired expired-record cleaner.
- skeptic 反驳：none found — no static caller, no Protocol/ABC contract requiring the method (SqlAlchemyIdempotencyRepository is a plain class), no dynamic dispatch (getattr/importlib/globals/hasattr/__all__), no FastAPI/Temporal/Alembic/SQLAlchemy-event/pytest-fixture registration, no cron/scheduler/worker wiring (apps/worker has zero cleanup/idempotency references), and no config/CI/shell reference. The only adjacent signal is docs/audit/spec-gap-audit-2026-06-11.json, which independently flags the MISSING cleanup job as a gap — i.e. it confirms the sweeper is built-but-never-wired rather than secretly invoked. Residual risk is 'low' (not 'none') only because this is a public repository method that is plausible future-infra a cron could wire up; deletion itself is behavior-neutral today.
- **建议改动**：Delete the cleanup_expired method (and the now-unused 'delete' import from sqlalchemy at line 5 if nothing else uses it).
- 复核执行的检查（10 项）：Read full sqlalchemy_idempotency.py (lines 1-59) to confirm method body and import usage；grep 'cleanup_expired' across all .py repo-wide: only its own definition；grep 'cleanup_expired'/'cleanup' across .yml/.yaml/.toml/.cfg/.ini/.sh/.json config+CI: no cron/scheduler reference；grep SqlAlchemyIdempotencyRepository/IdempotencyRepository/idempotency_repository: wired in apps/api only via app.py + common.py + dependencies.py:130 (.get/.put path)；Checked for Protocol/ABC interface declaring cleanup_expired: none — plain class, no base requiring it；grep for dynamic dispatch getattr/__all__/importlib/globals()/hasattr near idempotency/cleanup: zero hits…

### [production-1] `packages/production/sqlalchemy_mappers.py`:205-217  —  publish_package_row_to_contract
- 类别/类型：dead · unused function
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：high  · 残余风险：none
- 证据：0 external references. grep over ./packages ./apps ./tests shows this definition is never imported: packages/production/sqlalchemy_repository.py imports publish_record_row_to_contract (not publish_package) and never references this symbol; packages/publishing/sqlalchemy_repository.py imports publish_package_row_to_contract from packages.publishing.sqlalchemy_mappers (its own duplicate copy at packages/publishing/sqlalchemy_mappers.py:37), not from production. The production module has no __all__, so it is not a re-export. It is a dead duplicate of the publishing-package mapper.
- skeptic 反驳：none found
- **建议改动**：Delete the unused function publish_package_row_to_contract (lines 205-217); PublishPackage/PublishPackageRow imports become unused too if nothing else needs them (PublishPackage is unused after removal; PublishPackageRow already only used here).
- 复核执行的检查（8 项）：grep whole-repo (all file types) for symbol publish_package_row_to_contract: only 4 hits = production def + publishing def + 5 call sites all bound to publishing's own copy; zero refs to production copy；Read production/sqlalchemy_repository.py import block (lines 92-104): imports publish_record_row_to_contract NOT publish_package_row_to_contract；Confirmed no __all__ in production/sqlalchemy_mappers.py (not a re-export)；Checked dynamic references: no getattr/globals()/importlib/__import__ touching this symbol; no wildcard (import *) imports; no module-alias imports of production.sqlalchemy_mappers；grep tests/ for symbol: zero references (defense:false consistent)；diff of production copy (lines 205-217) vs publishing copy (lines 37-49): IDENTICAL — true dead duplicate…

### [creative-1] `packages/planning/editing/audio_pause.py`:114-131  —  audio_pause_windows_to_payload
- 类别/类型：dead · unused function
- 对 main 分支有效性：**仅领先态(不在 main 分支)**
- 置信：high  · 残余风险：low
- 证据：0 引用：grep -rn audio_pause_windows_to_payload ./packages ./apps ./tests 仅命中其自身 def（第114行）。不在任何 __all__/__init__ 再导出中（packages/planning/editing/__init__.py 仅导出 build_semantic_audio_boundary_entries 等，未含此函数）。同模块的 match_audio_pause_window 才是 boundary.py 使用的；此 to_payload 形态无任何调用方，也无动态/框架引用。
- skeptic 反驳：none found — function has zero callers in any extension, no __all__/__init__ re-export, no getattr/string/importlib dynamic dispatch, no consumer of its payload shape, and git -S history shows it was never referenced after the single porting commit that introduced it. (Only speculative point: it is a 'ported faithfully' diagnostics-style helper that could have been intended as a future export, but it was never wired in — not an actual reference, so not a refutation.)
- **建议改动**：删除整个 audio_pause_windows_to_payload 函数（保留 match_audio_pause_window）。
- 复核执行的检查（10 项）：Located real file: worktree packages/planning/ is a stale/sparse checkout (only __init__.py); analyzed the live file at /home/nanzhi/projects/cutagent-genesis/packages/planning/editing/audio_pause.py (lines 114-131 match)；grep -rn audio_pause_windows_to_payload across whole repo all extensions (excluding .git/.venv/__pycache__/node_modules): only the def at line 114；Partial-symbol greps windows_to_payload and to_payload: only the def site；Read editing/__init__.py: __all__ does not include the symbol and audio_pause module is not even imported there; module has no own __all__; planning/__init__.py is an empty docstring with no re-exports；Dynamic-dispatch scan in packages/planning/editing/: no getattr/globals()/__dict__/importlib/vars()；Module-import consumer check: boundary.py does 'from packages.planning.editing import audio_pause' and only accesses audio_pause.match_audio_pause_window (line 90) — no attribute access to the payload fn…

### [creative-2] `packages/planning/editing/frame_grid.py`:92-94  —  FrameWindow.start_seconds
- 类别/类型：dead · unused property
- 对 main 分支有效性：**仅领先态(不在 main 分支)**
- 置信：medium  · 残余风险：low
- 证据：0 引用：grep -rn '\.start_seconds' ./packages ./apps ./tests 无任何命中（除本定义）。FrameWindow 被导出且被 plan.py/slice_windows 使用，但消费者只用 .length_frames / .start_frame / .end_frame；start_seconds 这个属性从未被读。属于‘忠实移植’遗留的完整 API 表面。
- skeptic 反驳：none found
- **建议改动**：删除 FrameWindow.start_seconds property（如同时删 end_seconds/duration_seconds，docstring 第80-82行对这三者的描述一并精简）。
- 复核执行的检查（9 项）：Read full frame_grid.py (174 lines) — confirmed start_seconds is a @property on frozen dataclass FrameWindow (lines 92-94), pure derived value to_seconds(start_frame), deleting changes no behavior of any other code path；grep -rn '\.start_seconds' ./packages ./apps ./tests — 0 hits except definition；grep -rn 'start_seconds' whole repo (all extensions, not just .py) — only the definition+docstring; all other *_seconds hits are unrelated identifiers (timeline_start_seconds, source_start_seconds, source_window_start_seconds) or Prometheus metric names (api_request_duration_seconds etc.)；Dynamic-access sweep: grep for getattr/asdict/astuple/__dict__/vars( touching window/frame/second — 0 hits；String-key serialization sweep: grep for quoted 'start_seconds'/'end_seconds'/'duration_seconds' — 0 hits；Framework-registration check: FrameWindow re-exported via __init__.py __all__, but it is a plain dataclass property — no FastAPI route, Temporal @activity/@workflow, Alembic, Pydantic/SQLAlchemy event, pytest fixture, or entry_point references it…

### [creative-3] `packages/planning/editing/frame_grid.py`:96-98  —  FrameWindow.end_seconds
- 类别/类型：dead · unused property
- 对 main 分支有效性：**仅领先态(不在 main 分支)**
- 置信：medium  · 残余风险：low
- 证据：0 引用：grep -rn '\.end_seconds' ./packages ./apps ./tests 无任何命中（除本定义）。与 start_seconds 同属 FrameWindow 移植遗留的未用便捷属性；消费者只用 frame 索引与 length_frames。
- skeptic 反驳：none found
- **建议改动**：删除 FrameWindow.end_seconds property。
- 复核执行的检查（8 项）：Located actual file at /home/nanzhi/projects/cutagent-genesis/packages/planning/editing/frame_grid.py (candidate's grep path .claude/worktrees/repo-cleanup/... was wrong/empty — re-ran against real repo root)；grep -rn '\.end_seconds' over all *.py: zero hits outside the definition；grep -rn '\bend_seconds\b' excluding source_window_end_seconds & frame_grid.py: zero hits (the plan.py 'timeline_end_seconds' and 'source_window_end_seconds' are different symbols)；Whole-repo grep 'end_seconds' across ALL files (--exclude-dir=.git, incl json/yaml/md/j2): only the line-97 def and a line-81 docstring mention；Dynamic-access sweep: grep getattr/asdict/astuple/__dict__/vars over window/frame/seconds — only unrelated shots.py video.frame_rate getattr；Export/registration check: FrameWindow is re-exported in __init__.py __all__, but it's the class that's exported, not the property; no consumer reads .end_seconds; sibling .start_seconds is identically unused…

### [creative-4] `packages/planning/editing/frame_grid.py`:100-102  —  FrameWindow.duration_seconds
- 类别/类型：dead · unused property
- 对 main 分支有效性：**仅领先态(不在 main 分支)**
- 置信：medium  · 残余风险：low
- 证据：0 引用：grep -rn '\.duration_seconds' ./packages ./apps ./tests 无任何命中（除本定义）。它内部引用 self.length_frames，但 length_frames 另有 plan.py 与多处测试使用、不受影响；duration_seconds 本身无任何外部读者。
- skeptic 反驳：none found — FrameWindow is re-exported via __all__ in packages/planning/editing/__init__.py, but __all__ only exports the class name; a property is not an independently exported module-level symbol, so duration_seconds has no protected public surface. No internal, test, dynamic, string, or serialization reader exists.
- **建议改动**：删除 FrameWindow.duration_seconds property（length_frames 保留）。
- 复核执行的检查（10 项）：Read frame_grid.py:100-102 — confirmed property is the only definition site, redundant with existing start_seconds/end_seconds/length_frames；grep -rn '.duration_seconds' (attribute access) across whole repo — 0 hits besides the definition；grep -rn 'duration_seconds' across ALL files (any ext, incl docs/json/md/templates) — all other hits are the unrelated Prometheus metrics namespace in core/observability/telemetry.py；grep for quoted '"duration_seconds"' / string getattr access — 0 hits to FrameWindow.duration_seconds；grep for getattr/__dict__/vars/asdict/astuple/dataclasses.fields/model_dump/.dict over frame/window — none touch FrameWindow；grep FrameWindow usages repo-wide — used only in frame_grid.py internals + re-exported in __init__.py __all__ (class name only)…

### [ops-publishing-1] `packages/ops/balance/providers/openai_relay.py`:13  —  datetime
- 类别/类型：dead · unused import
- 对 main 分支有效性：**仅领先态(不在 main 分支)**
- 置信：high  · 残余风险：none
- 证据：`from datetime import datetime, timedelta`，但 `grep -n datetime` 仅命中第 13 行 import 自身，函数体只用了 `timedelta`（第 32-33 行）。ruff F401 命中：`datetime.datetime imported but unused`，0 处使用。
- skeptic 反驳：none found
- **建议改动**：改为 `from datetime import timedelta`，删除未用的 `datetime`。
- 复核执行的检查（9 项）：Read packages/ops/balance/providers/openai_relay.py (shared checkout) — confirmed datetime only on import line 13; timedelta used at lines 32-33；grep -n datetime in file: sole hit is the import；ruff F401: confirms `datetime.datetime imported but unused`, 1 fixable, 0 uses；grep repo-wide for `from ...openai_relay import` / `import openai_relay`: only OpenAIRelayPoller imported in registry.py:26; datetime never re-exported through this module；grep for attribute access `openai_relay.datetime`: none；Checked __all__ in module: absent; providers/__init__.py is empty (no re-export machinery)…

### [migrations-1] `packages/migrations/legacy_asset_utils.py`:123-134  —  font_path
- 类别/类型：dead · unused function
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：high  · 残余风险：low
- 证据：grep across ./packages ./apps ./tests ./scripts 找到 0 个调用者（仅函数定义本身；第124行的 item.get("font_path") 是字典键字符串，非对该函数的引用）。font 采集逻辑已被 _collect_fonts 桩化为 no-op，留下该 helper 无人调用。
- skeptic 反驳：none found
- **建议改动**：删除整个 font_path 函数（123-134行）。
- 复核执行的检查（8 项）：Read packages/migrations/legacy_asset_utils.py — confirmed font_path (123-134) is a standalone module-level function; its only same-file hit (line 124) is item.get("font_path"), a dict-key string literal, not a self-call；grep -rn font_path across whole repo (.py and non-.py) — only 2 hits total, both inside the function being deleted; no callers anywhere；Read the import block in the sole consumer packages/migrations/legacy_assets.py (lines 9-21) — confirmed font_path and font_records are NOT imported；Read _collect_fonts (legacy_assets.py 212-215) — confirmed it is a verified no-op stub that never references font_path/font_records；Checked dynamic/framework references: __all__, getattr/globals/importlib, import *, import legacy_asset_utils, .font_path( attribute calls — all absent；Checked entry_points/setuptools registration in pyproject.toml/setup.cfg/setup.py — no reference to the module/symbol…

### [migrations-2] `packages/migrations/legacy_asset_utils.py`:109-120  —  font_records
- 类别/类型：dead · unused function
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：high  · 残余风险：none
- 证据：grep 全仓（含 scripts）仅命中定义与第120行的自递归 yield from font_records(...)，0 个外部调用者。vulture 因递归自调用未标记，但实际无人入口调用。与 font_path 同因 _collect_fonts no-op 化而孤立。
- skeptic 反驳：none found
- **建议改动**：删除整个 font_records 生成器函数（109-120行）。
- 复核执行的检查（11 项）：Read full target file packages/migrations/legacy_asset_utils.py (font_records lines 109-120)；grep -rn font_records across entire repo (all files, not just .py): only def at 109 + self-recursive yield-from at 120；grep -n __all__ in target file: none present, so no re-export；grep -rn legacy_asset_utils importers (.py): legacy_assets.py / legacy_asset_clients.py / scripts/migrate_legacy_assets.py — none import font_records；Read legacy_assets.py import block (lines 9-21): font_records NOT in import list；Read _collect_fonts (legacy_assets.py:212-215): confirmed no-op, returns immediately, never calls font_records/font_path…

### [migrations-3] `packages/migrations/legacy_assets.py`:312-313  —  _key_from_uri
- 类别/类型：dead · unused method
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：high  · 残余风险：none
- 证据：vulture 60% 命中；grep ./packages ./apps ./tests ./scripts 仅命中定义本身，0 调用者。私有方法（下划线前缀）无动态/框架引用可能。同一处的 urlsplit 在 _oss_key(305行) 已被使用，但 _key_from_uri 自身无人调用。
- skeptic 反驳：none found
- **建议改动**：删除 _key_from_uri 方法（312-313行）。
- 复核执行的检查（8 项）：Read legacy_assets.py lines 280-313 to confirm method body and surrounding context；grep -rn _key_from_uri across all *.py — only the definition line matches, 0 callers；grep -rn key_from_uri across ALL file extensions (incl strings) — only definition matches；Checked for dynamic dispatch in file: getattr/__getattr__/globals()/importlib/__all__/setattr — only one getattr at line 345 which dispatches on self.import_client (a collaborator), not the migrator class；Verified LegacyAssetMigrator is never subclassed anywhere (no (LegacyAssetMigrator) base) — rules out override hook；Confirmed test files (tests/scripts/test_legacy_asset_templates.py, test_migrate_legacy_assets.py) and scripts/migrate_legacy_assets.py import only the class/MigrationResult/run_migration, none call _key_from_uri…

### [web-1] `apps/web/src/styles.css`:1  —  (文件)
- 类别/类型：dead · unused file
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：high  · 残余风险：low
- 证据：knip 报 Unused file。全仓 grep 'styles.css' 0 引用；main.tsx 只 import './index.css'，index.css 才是真正入口。styles.css 无任何 import/link 引用。
- skeptic 反驳：none found — styles.css is never imported/linked anywhere. main.tsx imports only ./index.css; index.html scripts only main.tsx; no import.meta.glob/require/@import pulls it in; vite has no publicDir/glob trick; the sole repo-wide "styles.css" hit is descriptive prose in docs/audit/spec-gap-audit-2026-06-11.json, not code. It is the superseded old design system (commit 96dd401), replaced by index.css in the redesign (a7aef6b). One nuance, but it does NOT block deletion: LoginPage.tsx still references loginPage/loginPanel/loginMark which live only in styles.css — but since styles.css is never loaded, those classes are ALREADY unstyled at runtime today, so deletion introduces zero behavior change (the real fix, if any, belongs in LoginPage, not in keeping a never-loaded file). Hence low (not none) residual risk: deletion is safe but it exposes a pre-existing stale-classname smell in LoginPage that someone should clean up separately.
- **建议改动**：删除 src/styles.css 整个文件
- 复核执行的检查（8 项）：全仓 grep 'styles.css'：仅 docs/audit JSON 1 处描述性引用，无代码引用；main.tsx 仅 import './index.css'；index.html 仅加载 main.tsx；grep import.meta.glob/require(css)/@import：无动态或 glob 引入 styles.css；vite.config 检查 publicDir/glob/css include：无间接打包 styles.css；对比 index.css vs styles.css 类选择器：styles.css 有 31 个独有类，但确认是旧设计系统遗留（.shell/.sidebar/.navItem 等），现役组件用 index.css 的 app-shell/nav-item；TSX 使用核查：所谓命中（shell/toast/brand）多为 Tailwind 子串误报；standalone className="shell"/"toast" 计数为 0…

### [web-2] `apps/web/src/components/ui/Skeleton.tsx`:5-13  —  SkeletonCard
- 类别/类型：dead · unused export (function)
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：high  · 残余风险：none
- 证据：knip 报 Unused export。grep 'SkeletonCard' 仅命中其定义行，0 处引用。同文件的 Skeleton 仍被多处使用（knip 未标），只有 SkeletonCard 死。
- skeptic 反驳：none found
- **建议改动**：删除 SkeletonCard 函数（保留 Skeleton）
- 复核执行的检查（9 项）：Read full Skeleton.tsx: SkeletonCard (lines 5-13) is a self-contained presentational fn component; only consumes the sibling Skeleton export, no side effects on import；grep 'SkeletonCard' repo-wide (ts/tsx/js/jsx/json/md/mdx): single hit = its own definition line, 0 references；case-insensitive grep 'skeletoncard': only the definition；Checked apps/web/src/components/ui/ dir listing: NO index.ts/barrel file exists; nothing re-exports SkeletonCard；grep 'export *' in ui dir: none (no wildcard re-export that could surface it)；grep 'ui/Skeleton' importers: only OverviewStatCards.tsx and RecentRunsList.tsx, both import { Skeleton } (named) not SkeletonCard -> Skeleton stays alive, correctly preserved…

### [web-9] `apps/web/src/api/client.ts`:689  —  CaseDetail
- 类别/类型：dead · unused exported type alias
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：low  · 残余风险：low
- 证据：knip 报 Unused exported type。grep 'CaseDetail' 在 src 内除定义行、schema.d.ts、openapi.json 外 0 处 import。该块多数兄弟别名（RunCard 22、MediaAssetRecord 18 等）都在用，说明是精选再导出列表而非整表镜像，故此别名确实未用。但属 API 契约类型表面，skeptic 可能认为是有意保留的公共 API，故低置信。
- skeptic 反驳：none found — 这是手工精选的 re-export 别名列表里的一条，多数兄弟别名（CaseListItem、RunCard、MediaAssetRecord 等）确有组件 import，但 CaseDetail 本身在 apps/web/src 全量 0 处 import。唯一接近的"反驳"是它属 API 契约表面、未来组件可能想 import，但这是语义判断而非确凿引用；删除后 tsc --noEmit 退出码 0，无任何消费者断裂。lowercase caseDetail 局部变量（useQuery 结果）的类型经 api.cases.detail 的返回类型 JsonResponse<operations[...]> 推断得到，不经此别名。
- **建议改动**：删除 'export type CaseDetail = ...' 一行（确认无前端契约/未来用途后）
- 复核执行的检查（7 项）：grep 'CaseDetail' 全仓（.ts/.tsx/.js/.jsx/.json/.vue/.svelte，排除 node_modules/dist/.git）——仅命中 client.ts:689 定义行 + 生成文件 schema.d.ts/openapi.json + audit docs 中引用后端 Python 同名类型；grep barrel/wildcard re-export（export * from / from '@/api/client'）——无 wildcard 再导出该别名；Read client.ts:217-235 确认 api.cases.detail 返回 JsonResponse<operations['case_detail_api_cases__case_id__get']>，不经 CaseDetail 别名；Read StudioCreatePage.tsx 确认 caseDetail useQuery 的类型靠推断而非显式 import CaseDetail；其 import 行不含 CaseDetail；case-insensitive grep 区分 lowercase caseDetail 局部变量 vs 类型别名；查找 knip 配置文件（knip.* / .kniprc* / package.json）——无配置文件，排除 ignoreExportsUsedInFile 等误报来源…

### [web-10] `apps/web/src/api/client.ts`:694  —  WorkflowRun
- 类别/类型：dead · unused exported type alias
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：low  · 残余风险：low
- 证据：knip 报 Unused exported type。grep 'WorkflowRun' 在 src 内除定义行、schema.d.ts、openapi.json 外 0 处 import（schema.d.ts 的命中都是 components["schemas"]["WorkflowRun"] 内部引用，与 client.ts 的别名无关）。同 web-9，属 API 契约表面，低置信。
- skeptic 反驳：none found
- **建议改动**：删除 'export type WorkflowRun = ...' 一行（确认无契约/未来用途后）
- 复核执行的检查（8 项）：全仓 grep 'WorkflowRun'（.ts/.tsx/.js/.jsx/.vue/.json/.md）：src 内除 client.ts:694 定义行外无任何对该别名的引用；word-boundary grep '\bWorkflowRun\b' in apps/web/src（排除 WorkflowRunResponse/WorkflowRuntime/schema.d.ts/openapi.json/client.ts定义行）：0 命中 (exit 1)；grep 所有 import from '.../api/client'：WorkflowRun 不在任何 import 列表（同块 NodeRun/RunCard 等则被 import）；检查 wildcard再导出 'export * from client' 与命名再导出 'export {} from client'：均无；grep 测试文件 *.test.ts/*.tsx/*.spec.ts 与 src/contracts/*.typecheck.ts 对 WorkflowRun 的引用：0 命中；Read client.ts 头部与别名块：手维护文件、别名按需 curated、无 keep-all 不变量；多个兄弟别名也未被使用…


## A2 · AI slop（6）

### [migrations-4] `packages/migrations/legacy_assets.py`:212-215  —  _collect_fonts
- 类别/类型：slop · 冗余 guard / 桩残留
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：low  · 残余风险：none
- 证据：方法体为 `if "font" not in selected: return` 紧跟一个无条件 `return`，两条分支都返回 None、无任何副作用——该 if 判断完全多余，删除后行为完全一致（方法仍是 no-op）。font no-op 行为由 test_font_kind_is_clean_noop_without_reading_legacy_font_metadata 覆盖。仅删冗余分支不改变行为；不建议删整个方法（仍被 run() 第84行调用，删方法会改结构）。
- skeptic 反驳：none found — git blame proves this is a stub residual (commit cec57ef "...drop font" gutted the real body added by f04cc11, leaving an orphaned `if "font" not in selected: return` guard above a bare `return`). Both branches return None with zero side effects, so the guard is provably dead. I empirically applied the exact proposed simplification (collapse to a single `return`) and ran the full test module (7 tests pass) plus the named test test_font_kind_is_clean_noop_without_reading_legacy_font_metadata (passes), which pins the no-op contract (media_rows==[], rows_by_kind=={}, warnings==[], oss.json_requested==[], oss.list_requested==[], oss.checked==[]). No dynamic/getattr/string references to _collect_fonts; it is private and called exactly once (line 84). Caveat (non-blocking): legacy_asset_utils.font_records/font_path remain live, hinting font support may return later, but that does not affect collapsing this redundant guard.
- **建议改动**：将方法体的 `if "font" not in selected:\n    return\n    return` 简化为单个 `return`（或 `pass`），删去多余的 if 分支。
- 复核执行的检查（6 项）：Read packages/migrations/legacy_assets.py full file — confirmed _collect_fonts body (lines 212-215) is `if "font" not in selected: return` + unconditional `return`, both no-op；grep -rn _collect_fonts across repo — only def (212), single call site run() line 84; no dynamic/getattr/string/__all__ references；git blame -L 212,215 + git log — confirmed stub residual: f04cc11 added real body, cec57ef 'drop font' replaced it with bare return leaving dead guard；Read the named test (tests/scripts/test_migrate_legacy_assets.py:315) — confirmed it exercises kinds={'font'} and asserts full no-op (media_rows/rows_by_kind/warnings empty, no oss json/list/checked calls)；Applied the exact proposed simplification to a working copy and ran pytest tests/scripts/test_migrate_legacy_assets.py (7 passed) + the specific font no-op test (passed); then restored original — git status clean；Checked legacy_asset_utils.py for residual font logic (font_records/font_path still present) — noted as non-blocking caveat, does not affect this guard

### [web-4] `apps/web/src/components/publish/publishModel.ts`:128-131  —  toDisplayUrl
- 类别/类型：slop · 纯透传包装函数 + 复述式重复注释
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：low  · 残余风险：low
- 证据：与 web-3 同型：函数体仅 'return sanitizeDisplayUrl(url)'，sanitizeDisplayUrl 即 lib/url 的 toDisplayUrl；JSDoc 与 lib/url.ts:1 逐字相同。CoverPanel 经此重导出引用。删除需改 CoverPanel import 来源，非纯零行为删除，置信度偏低。
- skeptic 反驳：none found
- **建议改动**：删掉本地 toDisplayUrl 包装与重复注释，CoverPanel 直接从 lib/url 导入
- 复核执行的检查（3 项）：grep references；read source；read consumer

### [web-5] `apps/web/src/components/analytics/analyticsModel.ts`:29  —  moneyAmount
- 类别/类型：slop · 冗余 export 关键字（符号本身在用）
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：medium  · 残余风险：low
- 证据：knip 报 Unused export。grep 确认 moneyAmount 仅在 analyticsModel.ts 内部被 formatMoney 等调用（行 35/110/119），无任何外部 import。函数非死代码，但 export 关键字多余；去掉 export 不改变任何行为。
- skeptic 反驳：none found
- **建议改动**：去掉 moneyAmount 的 export 关键字（保留函数为模块内私有）
- 复核执行的检查（7 项）：Read full analyticsModel.ts to confirm moneyAmount is a live function called internally at lines 35/110/119 (formatMoney, buildCostBars, usageHasData)；Full-repo grep for moneyAmount across ts/tsx/js/jsx/vue (excluding node_modules): only the 4 in-file occurrences, zero external imports；Grep for 'from ...analyticsModel' and 'analyticsModel' across all importers: 8 files import OTHER symbols (formatMoney, buildCostBars, etc.), none import moneyAmount；Checked analytics dir for barrel/index.ts and 'export *' re-exports: none exist；Searched for test/spec/snapshot files under apps/web and repo-wide refs in md/json/snap: zero (no test files exist under apps/web)；Confirmed no knip.json/.kniprc config and no knip script in package.json (finding is advisory only, nothing depends on the export)…

### [web-6] `apps/web/src/components/analytics/analyticsModel.ts`:60  —  latestWorkflowEvents
- 类别/类型：slop · 冗余 export 关键字（符号本身在用）
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：medium  · 残余风险：low
- 证据：knip 报 Unused export。grep 确认仅在本文件内部被调用（行 76/93），无外部 import。去掉 export 不改变行为。
- skeptic 反驳：none found
- **建议改动**：去掉 latestWorkflowEvents 的 export 关键字
- 复核执行的检查（7 项）：Read full analyticsModel.ts to confirm latestWorkflowEvents is defined at line 60 and called internally at lines 76 (summarizeWorkflowStats) and 93 (buildFunnelSteps)；grep -rn latestWorkflowEvents across entire repo with TS/TSX/JS filters (only 3 hits, all in analyticsModel.ts)；Broad grep (no file-type filter) for latestWorkflowEvents incl. string/dynamic refs — no external/dynamic references；Searched for analyticsModel imports across web app: confirmed no module imports latestWorkflowEvents (only summarizeWorkflowStats/buildFunnelSteps/etc. are imported, which call it internally)；Checked analytics dir for index.ts barrel and export * star re-exports — none exist；Searched .test/.spec files for analyticsModel/latestWorkflowEvents/summarizeWorkflowStats/buildFunnelSteps — no test references…

### [web-7] `apps/web/src/components/studio-create/studioCreateModel.ts`:30  —  defaultForm
- 类别/类型：slop · 冗余 export 关键字（符号本身在用）
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：medium  · 残余风险：low
- 证据：knip 报 Unused export。grep 确认 defaultForm 仅在本文件内部被多处引用（行 75-95），无外部 import。去掉 export 不改变行为。
- skeptic 反驳：none found — no external importer, no barrel/export*, no string/dynamic access, no test references. The only theoretical refutation (knip false-positive via barrel re-export) does not apply: studio-create/ has no index.ts and no `export *`. Note the action must be narrowing-only (drop the `export` keyword), NOT deleting the symbol — defaultForm is live and used internally by loadStoredForm at lines 75-95.
- **建议改动**：去掉 defaultForm 的 export 关键字
- 复核执行的检查（7 项）：Read full studioCreateModel.ts: defaultForm is a value export used internally at lines 75,78,81,83-86,88,91,95 inside loadStoredForm()；Repo-wide grep for 'defaultForm' across ts/tsx/js/jsx/json/md/vue/svelte: only hits are inside studioCreateModel.ts itself；Grep for importers of studioCreateModel: exactly 2 (StudioCreateSteps.tsx, StudioCreatePage.tsx); inspected both import lists — defaultForm NOT imported by either；Checked studio-create/ for index.ts barrel and 'export *' re-exports: none exist；Checked apps/web for app-level *.test/*.spec files referencing defaultForm: none (only unrelated node_modules tests)；Grep for string-keyed/dynamic/bracket-property access ("defaultForm", 'defaultForm', [..defaultForm..]): none…

### [web-8] `apps/web/src/hooks/useUpload.ts`:4  —  UploadStage
- 类别/类型：slop · 冗余 export 关键字（类型本身在用）
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：medium  · 残余风险：none
- 证据：knip 报 Unused exported type。grep 确认 UploadStage 仅在本文件内部用作 UploadState.status 的类型（行 15），无外部 import。去掉 export 不改变行为。
- skeptic 反驳：none found
- **建议改动**：去掉 UploadStage 的 export 关键字
- 复核执行的检查（8 项）：Read useUpload.ts full context — confirmed UploadStage used internally at line 15 as UploadState.status; removing only the export keyword leaves the type in use (simplification, not deletion)；grep -rn UploadStage across all .ts/.tsx (and case-insensitive across whole repo) — appears only in useUpload.ts:4 and :15; no external import；Checked all useUpload consumers (LibraryAssetUploadModal, TemplateUploadModal, VoiceModals, CoverPanel, SourceStep, TemplatesTab, m6aR3/m6aR4 typecheck) — all import only useUpload, none import the UploadStage type; they access upload.status structurally；Verified contract typecheck m6aR3.typecheck.ts:51 validates upload.status against an INLINE string-literal union, not against the named UploadStage type — no dependency on the export；Searched for barrel/index re-export of useUpload and for __all__-equivalent re-export — none found; hooks dir has no index.ts；Searched for dynamic/string-literal references to UploadStage in non-ts files (json, fixtures) — none found…


## A3 · 过度防御 defense（0）

（无）


---
# B. 待人工确认 flag-only（skeptic 有疑点，需你拍板）

## B · flagged（3）

### [core-contracts-2] `packages/core/contracts/__init__.py`:11-13  —  (文件)
- 类别/类型：slop · comment justifying dead code with a false claim
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：high  · 残余风险：low
- 证据：Comment asserts callers rely on `packages.core.contracts.Decimal` (transitive re-exports), but no such caller exists anywhere in ./packages ./apps ./tests (0 grep hits). The comment only exists to justify the dead import block in finding core-contracts-1; removing it changes no behavior.
- skeptic 反驳：候选对注释的定性不准确，且改动范围不自洽。(1) 第11行注释实际内容是 `# Re-exported standard/third-party symbols (preserved for backward-compatible imports).`，它是 12-30 行整个 stdlib+pydantic 再导出块的通用块头，并非"声称某个调用方现在依赖 contracts.Decimal"的虚假断言——证据里"comment justifying dead code with a false claim"属于误读。(2) AST 全仓扫描证明：整段再导出块（datetime/timedelta/timezone/Decimal/Enum/Annotated/Any/Generic/Literal/TypeVar/uuid4/BaseModel/ConfigDict/Field/JsonValue/model_validator）对外部调用方同样零引用，候选却只删块头注释+datetime三元组+Decimal（11-13行），保留下面18个同样未被使用的再导出，删后文件更不一致、并使 3-9 行的设计说明注释悬空。(3) 该 `__init__` 是 commit 47abb15 SSOT 拆分时刻意建立的向后兼容公共导入面（lines 3-9 明确说明意图），单独裁掉 Decimal 属于"是否收窄公共 API 面"的语义判断，不是显然安全的 slop 删除。技术上孤立删 Decimal 今天不会破坏任何消费者（AST/grouped-import/属性访问/getattr/importlib/__all__/star-import/introspection 全部 0 命中），但定性错误+范围不自洽，应人工确认。
- **建议改动**：Remove the 3-line comment along with the dead import block it documents.
- 复核执行的检查（12 项）：Read packages/core/contracts/__init__.py 全文，确认 11-13 行是块头注释+datetime三元组+Decimal，注释非 noqa/type:ignore/pragma/license 头；grep contracts.Decimal / from packages.core.contracts import Decimal 全仓（.py/.ipynb/.md/.cfg/.ini）= 0 命中；AST 解析全仓所有 ImportFrom，逐符号确认 datetime/Decimal/Enum/BaseModel 等 16 个 stdlib+pydantic 再导出对外部调用方 used=False；正则扫描 grouped(多行) contracts import 体，确认无任何 stdlib/3rd-party 符号经 contracts 拉取 = NONE；属性访问 grep: contracts.<Decimal|datetime|...> = NONE；动态访问 grep: getattr/importlib/import_module(contracts) = NONE…

### [creative-5] `packages/planning/editing/plan.py`:70-76  —  PlannedSegment.timeline_start_seconds / timeline_end_seconds
- 类别/类型：dead · unused property
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：medium  · 残余风险：medium
- 证据：0 引用：grep -rn '\.timeline_start_seconds' 与 '\.timeline_end_seconds' ./packages ./apps ./tests 均无任何命中（除本定义）。对比同类的 PlannedSegment.timeline_length_frames（plan.py:66）在 tests/planning/test_editing_planner.py 第192/212/341/342 行被断言使用、故保留；而这两个 *_seconds 属性的便捷换算从未被读，PlannedSegment 由 portrait_planning.py 消费但只取 frame 字段。
- skeptic 反驳：候选所描述的目标在当前仓库不存在，整条候选基于错误的树状态生成，不可直接执行。具体证据：(1) 文件 packages/planning/editing/plan.py 在当前工作树与 HEAD 都不存在——`git cat-file -e HEAD:packages/planning/editing/plan.py` 返回 "path does not exist in HEAD"；packages/planning/ 目录下只有 __init__.py。(2) 类 PlannedSegment、符号 timeline_start_seconds / timeline_end_seconds / timeline_length_frames 在全仓任何文件中均 0 命中（含非 .py）。(3) 候选所称的消费者 portrait_planning.py 和测试 tests/planning/test_editing_planner.py 都不存在（find 无结果）。(4) 该文件仅存在于历史提交 111b3ab（feat(planning/editing): port ... step 2a），而 `git merge-base --is-ancestor 111b3ab HEAD` 判定其 NOT ancestor——即从未合并进当前分支。结论：没有可删的代码，"删除两个 property" 无法落地；候选是针对另一分支/旧状态生成的陈旧条目，需人工核对快照来源后再处理，绝不能盲目当作 already-clean 通过。
- **建议改动**：删除 PlannedSegment.timeline_start_seconds 与 timeline_end_seconds 两个 property（保留 timeline_length_frames）。
- 复核执行的检查（9 项）：全仓 grep 符号 timeline_start_seconds / timeline_end_seconds（--include=*.py 及全文件类型）：0 命中；全仓 grep PlannedSegment / timeline_length_frames：0 命中；ls packages/planning/ 与 packages/planning/editing/：仅有 __init__.py，editing 子目录不存在；git ls-files | grep plan.py：仅 tests/workflow/test_reuse_plan.py，无 editing/plan.py；git cat-file -e HEAD:packages/planning/editing/plan.py：path does not exist in HEAD；find portrait_planning.py / test_editing_planner.py：均无结果…

### [web-3] `apps/web/src/components/library/libraryModel.ts`:101-104  —  toDisplayUrl
- 类别/类型：slop · 纯透传包装函数 + 复述式重复注释
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：low  · 残余风险：low
- 证据：该函数体仅 'return sanitizeDisplayUrl(url)'，sanitizeDisplayUrl 就是 import 自 lib/url 的 toDisplayUrl（import { toDisplayUrl as sanitizeDisplayUrl }）。函数无任何附加逻辑，且上方 JSDoc 注释与 lib/url.ts:1 逐字相同（三处完全一致的复制）。属模板化冗余。注意：删除会改动 BgmTab/FontsTab/TemplatesTab 的 import 来源，非纯零行为删除，故置信度偏低。
- skeptic 反驳：存在确凿的同构平行约定：apps/web/src/components/publish/publishModel.ts:129-131 有一份逐字相同的包装函数（同样 `return sanitizeDisplayUrl(url)`、同样 import 别名、同样的复述 JSDoc 注释）。两个独立的 feature *Model.ts 都把 lib/url.toDisplayUrl 以本地名重导出，这不像偶然复制的废料，而更像有意的"feature-local facade"约定——BgmTab/FontsTab/TemplatesTab 把 collectUsefulTags、templateKindLabels、toDisplayUrl 等全部从单一 libraryModel barrel 导入。只删 library 这一份会与 publishModel 不一致，并迫使这些 tab 从两个模块分别 import。此外候选自报"非纯零行为删除"：删除需改写 3 个消费文件、4 个调用点（TemplatesTab 有 183、315 两处），不属于 slop 类别要求的"可证明零改动"的纯注释/死块删除；函数体非空（是 live re-export，不是注释）。注释虽确为 lib/url.ts:1 的逐字复述、删之零行为，但它与函数删除耦合，整体是架构/风格判断而非死代码，应人工确认 facade 约定意图后再动。
- **建议改动**：删掉本地 toDisplayUrl 包装与重复注释，让 BgmTab/FontsTab/TemplatesTab 直接从 lib/url 导入 toDisplayUrl
- 复核执行的检查（8 项）：Read libraryModel.ts 全文确认 toDisplayUrl(101-104) 为纯透传 + 注释逐字复述 lib/url.ts:1；Read lib/url.ts 确认 toDisplayUrl 真实实现与签名一致；grep 全仓 toDisplayUrl 引用：消费者为 BgmTab/FontsTab/TemplatesTab（4 调用点），及 lib/url 直连方 EditorHandoffActions/RunDetailModal/RunsPage/CoverPanel；grep sanitizeDisplayUrl：仅 libraryModel 与 publishModel 两处别名 import；grep *.test/*.spec 中 toDisplayUrl/libraryModel：无任何测试覆盖该符号；grep libraryModel barrel 的全部导入方，确认无动态/再导出，全部为静态直接 import…


---
# C. 已否决 keep（对抗复核证伪，保守保留）

## C · dropped（3）

### [core-contracts-1] `packages/core/contracts/__init__.py`:14-20  —  datetime, timedelta, timezone, Decimal, Enum, Annotated, Any, Generic, Literal, TypeVar, uuid4, BaseModel, ConfigDict, Field, JsonValue, model_validator
- 类别/类型：dead · unused imports (dead re-exports)
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：high  · 残余风险：high
- 证据：ruff F401 flags all 16 names as imported-but-unused. None appear in __all__ (verified sed over lines 344+: 0 hits). The header comment claims 'some callers rely on e.g. packages.core.contracts.Decimal' but grep across ./packages ./apps ./tests for `contracts.<name>` attribute access and `from ...contracts import <name>` of every name returned 0 external references. No `import *` in the file, so they are not needed to populate a wildcard surface. Pure dead re-exports.
- skeptic 反驳：候选的两条核心证据都被证伪，且删除会破坏线上代码。(1) 主证据"ruff F401 标记全部16个名字"无法复现：在仓库根对该文件实际跑 `ruff check --select F401`（ruff 0.15.15）返回 "All checks passed!"——这些 import 用的是显式 `name as name` 冗余别名惯用法（第12-30行），ruff/PEP 484 将其识别为有意的公共再导出，文件注释第8-9行也正是这么写的。(2) "零外部引用"证伪：候选只查了 `from ...contracts import <name>` 和 `contracts.<name>` 两种模式，漏掉了主流的命名空间导入模式。仓库内 20+ 模块用 `from packages.core import contracts as c`，再以 `c.<name>` 访问。基于 AST、按文件解析别名的扫描，确认 `c.Decimal("0")` 在生产代码 apps/api/services/ops.py:55、:81 和 apps/api/services/providers.py:184 处被实际调用。删除该 import 块会使 packages.core.contracts.Decimal 抛 AttributeError，导致 ops provider-usage 成本聚合在运行时崩溃。该路径有测试覆盖（tests/api/test_provider_observability.py:106 命中 GET /api/ops/provider-usage-metrics）。
- **建议改动**：Delete the stdlib/pydantic re-export import block (lines 14-20) together with its now-false justifying comment (lines 11-13).
- 复核执行的检查（11 项）：实际执行 ruff check --select F401（该文件返回 All checks passed，证伪候选主证据）；查看 ruff 配置 pyproject.toml/ruff.toml；Read __init__.py 第1-60行确认 name-as-name 再导出惯用法及注释意图；grep 全仓 from packages.core.contracts import <16名字>（单行）；AST 解析全仓 packages/apps/tests 所有 ImportFrom，匹配 module==packages.core.contracts 的 16 个目标名（含多行 import）；AST 按文件解析 contracts 包别名（from packages.core import contracts as c / import ... as）并扫描 alias.<name> 属性访问——命中 c.Decimal…

### [api-services-1] `apps/api/services/imports.py`:292-298  —  _probe_import_media_if_local
- 类别/类型：defense · 宽泛 except 吞异常
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：low  · 残余风险：medium  · 测试覆盖：False
- 证据：函数对本地 URI 调用 probe_media，用裸 `except Exception: return None` 吞掉所有异常并把媒体当作不可探测处理。这会掩盖编程错误（如 local_object_path 抛 TypeError/ValueError、import 错误等）。该吞异常路径仅在非 s3:// 的本地 URI 且 probe_media 抛错时触发；现有 import 测试（tests/import/test_fresh_import.py 等）均用 s3:// URI，命中第 293 行 `if uri.startswith('s3://'): return None` 的短路提前返回，从不进入 try/probe 分支，因此 except 分支无测试覆盖。若要收紧，应只捕获 probe_media 的具体异常（如 FfmpegCommandError）而非裸 Exception。
- skeptic 反驳：该 except 分支无任何测试覆盖且收窄并非显然安全，两条 defense 准则均不满足。无覆盖：_probe_import_media_if_local 仅经 _create_imported_media_source_artifact 在 import_type==media(imports.py:62/87) 下可达，所有 media 导入测试都用 s3:// URI，命中 imports.py:293 短路 return None，从不进入 try/probe/except；唯一一条 local:// URI(tests/integration/test_sqlalchemy_production.py:47) 走 finished_video 分支(108)，不调用本函数。收窄不安全：local_object_path 经 parse_local_uri 对畸形 URI 及对象存储无法解析时均可抛 ValueError，若按建议收窄为 FfmpegCommandError/OSError，该 ValueError 会逃逸，构成无测试守护的行为回归。</refutation>
</invoke>

- **建议改动**：把 `except Exception` 收窄为 probe_media 实际抛出的具体异常类型（如 FfmpegCommandError / OSError），避免吞掉编程错误；删除/替换前需先补一条本地 URI 探针失败的测试。
- 复核执行的检查（9 项）：grep 全仓引用 _probe_import_media_if_local：仅 imports.py(def 292/call 222) 与 sqlalchemy_repository.py(twin)，无动态或框架注册引用；Read 候选函数 imports.py:292-298 及调用方 _create_imported_media_source_artifact:212-268；Read probe_media(ffmpeg.py:103-127) 确认抛 FfmpegCommandError；Read local_object_path(assets.py:13-21) 确认 parse_local_uri 与自身可抛 ValueError；追踪 import_type 路由：本函数仅 media 分支(imports.py:62/87)可达，finished_video 分支(108)不调用；grep tests/import 所有 URI scheme：仅 s3://（5 处）…

### [web-11] `apps/web/src/contracts/m6aA.typecheck.ts`:1  —  (文件)
- 类别/类型：dead · intentional compile-time assertion file (flag-only, keep)
- 对 main 分支有效性：**main 有效(行号一致)**
- 置信：high  · 残余风险：none
- 证据：knip 把 7 个 src/contracts/*.typecheck.ts 全报为 Unused files。这些是编译期类型断言文件（import api/契约类型并断言 shape，永不在运行时被 import），按设计就该'unused'。明确标为 keep/flag-only，不应当死代码删。其余 6 个同理：m6aR3/m6aR4/m6aR5/m6aR6/m6eB/m6eC.typecheck.ts。
- skeptic 反驳：这些 *.typecheck.ts 是 build 管线的有效组成部分，不是死代码。tsconfig.json 的 include:["src"] 会把 src/contracts/ 下所有 *.typecheck.ts 纳入编译；package.json build 脚本是 "tsc -b && vite build"；CI（.github/workflows/ci.yml:173）在 apps/web 目录执行 npm run build，因此 tsc -b 会实际类型检查这些文件。文件内通过 satisfies（m6aA 有 11 处）和带类型注解的赋值断言（m6eB/m6eC：如 const list: { items: PromptTemplateView[] } = await api.prompts.list(...)）来钉死 api/client 与 routes 的类型形状；一旦 API 客户端或路由类型漂移，tsc -b 会在 CI 构建期失败。它们按设计被 void 掉、运行时永不 import——这正是 knip 误报为 'Unused files' 的原因：knip 找不到 importer，但 tsc 仍会编译并校验它们。删除即移除唯一的编译期契约护栏，且无运行时测试可替代该保护。候选自身判断（keep/flag-only）正确。
- **建议改动**：保留全部 *.typecheck.ts（它们是有意的编译期契约护栏，勿删）
- 复核执行的检查（7 项）：Read m6aA.typecheck.ts 确认是 void 掉的编译期 satisfies 断言文件（无运行时调用）；ls apps/web/src/contracts/ 确认全部 7 个 *.typecheck.ts 同类；Read apps/web/tsconfig.json 确认 include:['src'] 覆盖 contracts 目录；Read apps/web/package.json 确认 build='tsc -b && vite build'；grep .github/workflows/ci.yml 确认 CI 在 apps/web 跑 npm run build（行 173）；grep -c satisfies 各文件：m6aA=11，m6aR3=11，m6aR4=12，m6aR5=14，m6aR6=1…


---
## 落地方式

批准某批后，对**目标基线**重跑：`Workflow({scriptPath:'.claude/workflows/repo-cleanup.js', args:{repoRoot:<目标checkout>, mode:'apply', approvedIds:[...], targetRoot:<同上>}})` → 逐条最小改动 → `pytest -q`（动前端加 `tsc --noEmit`）门控 → 全绿提交。

